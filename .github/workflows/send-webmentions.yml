name: Send Webmentions

on:
  workflow_dispatch:
  schedule:
    - cron: '0 * * * *'

jobs:
  send-webmentions:
    runs-on: ubuntu-latest

    steps:
    - name: Check out the repository
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: 14

    - name: Install dependencies
      run: |
        npm install @remy/webmention
      # or use 'yarn add xml2js' if you use Yarn

    - name: Send Webmentions
      run: npx webmention https://journal.robbi.my/index.xml --limit 10 --send --debug

